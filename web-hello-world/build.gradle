apply plugin: 'jetty'

configurations {
  provided
}

dependencies {
  provided 'javax.servlet:servlet-api:2.4'
  compile configurations.provided
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.0'
}
repositories {
  mavenCentral()
}
